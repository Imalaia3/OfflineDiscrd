import os

#how to add numbers to user we take advantage of return pnums so if nums=["233334", "346586586", "334345"] and usrnames=["Hello", "Test", "World"] (btw any username must have the VerifiedOffline and said the command %imoffline to come back type %imonline) then 233334 is Hello because the index is 0 in both of them use the same rule

pnums=["694473107"]
usrnames=["Imalaia3"]
# TODO: 0 is the first string

class getUserData():
    def getNumber(indx):
        return pnums[indx]




    def getUserName(indx):
        return usrnames[indx]
